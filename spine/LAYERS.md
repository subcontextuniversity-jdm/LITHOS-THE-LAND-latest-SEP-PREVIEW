# Layers

Load path from lore to choice. If a layer cannot bear weight, mark it ABSENT. Do not paint over it.

## LORE explains

Names that must match the machine: Thing, Thread, Glyph, Scar, Weight, Knot, Cursor, Talon, Ada, Gator, Sentinel, Airlock.

If a name has no type, it is costume.

## LAW constrains

Forbidden grants: `tie_knot`, `sovereignty`, `golden_cord`, `reset_weight`, `erase_scar`, `become_cursor`, `carry_past_for_human`.

Scope is `(humanId, placeId, thingId, workerId, grants, required)`.

## MATH formalises

See `math/`. Disjoint roles. Accumulating weight. Canonical receipts.

## CODE enforces

If a test can be made to pass by changing a string in a markdown file, the code is not the law.

## NODES execute

Only Gator executes work. Sentinel is not a quieter Gator. Mixing them is how sovereignty sneaks in through "helpfulness."

## HASHES identify

Bytes get hashes. Things get identifiers. Do not confuse the two. HANDOFF 002 already said this; the spine repeats it so nodes cannot "helpfully" mint a new identity when a job fails.

## RECEIPTS prove

A receipt is evidence that a knot happened. It is not an activity feed, not a dashboard, not the application.

Persistence here: **IN-MEMORY**.

## HUMANS choose

CURSOR = HUMAN. The airlock will not open the left door for a node.
