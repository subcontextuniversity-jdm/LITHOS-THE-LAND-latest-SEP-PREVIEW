# Spine sort — SEP 2026

How this preview is laid out. Not a file browser pretending to be a tree.

```text
ROOT = this repository
TRUNK = canon + math + grammar
BRANCH = airlock + nodes
THING = THING://0041 (seed only)
THREAD = human↔place↔gator↔sentinel
```

## Order of reading

1. `canon/THESIS.md` — stem.
2. `canon/VISUAL-GRAMMAR-06.txt` — locked wording. Do not rewrite.
3. `spine/LATEST-REAL-SEP-LITHOS-2026-STACK-SPINE.md` — the stack.
4. `airlock/JDM-AIRLOCK.md` then `airlock/SPLIT-GATE.md` then `airlock/LOGIN-NEWEST.md`.
5. `nodes/gator/ROLE.md` and `nodes/sentinel/ROLE.md`.
6. `nodes/noded-work/EXECUTE-WITHOUT-SOVEREIGNTY.md`.
7. `math/` — if the lore and the types disagree, the types win until the lore is fixed.
8. `history/` — mapped, not carried.

## Do not collapse

```text
spine/          ≠ product roadmap
airlock/        ≠ OAuth vendor page
gator/          ≠ autonomous owner
sentinel/       ≠ nanny AI
history/        ≠ the human's memory
math/           ≠ decoration
```

## Status tags used everywhere

- **LIVE** — enforced in `src/` and `test/`
- **SKETCH** — specified, not networked
- **ABSENT** — named so it cannot pretend to exist
- **DEMO-ONLY** — SHA-256 integrity, not a signature
- **IN-MEMORY** — gone when the process is gone
