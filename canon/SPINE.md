# LITHOS // SPINE — planted experiments, not PUBLIC HANDOFF 003

PUBLIC HANDOFF 003 is still not written. These are experiments on the map.

```text
HANDOFF 001 // BELONGING
Everything meaningful comes from somewhere.

HANDOFF 002 // CONTINUITY
Changing form does not require losing identity.

HANDOFF 003 // planted, not published
CAPABILITY
Something else can work on a Thing
without owning the Thing, the Place,
or the Human.
```

## Lock

Agents receive bounded capabilities, not inherited human credentials.

```text
Proton Pass knows the secrets.
Proton Mail knows identity/messages.
Slack knows the active conversation.
Lumo knows private context.
Google knows Workspace.
GitHub knows code.
Models know only the context they're handed.
LITHOS knows how all of those pieces relate.
```

Firefox is a model-facing surface. Proton is the credential/identity spine. The model is not the OS. It is a worker selected by the OS.

```text
ASK → ROUTE → MODEL → RESULT → REVIEW → RECEIPT
```

```text
TALON     = route / fork / return
SENTINEL  = boundary / permission
GLASS     = visibility / review
FORGE     = build / code
LUMO      = private thinking
```

A connector passport records that a capability exists and what it may do.
It never stores the password.

DON'T PULL THE GOLDEN CORD.
DON'T CUT THE THREAD.
