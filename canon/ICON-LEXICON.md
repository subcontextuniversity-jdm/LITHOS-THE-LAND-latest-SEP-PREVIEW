# ICON//LEXICON — this slice only

SCHEMA → ENTITY → GLYPH → VALIDATOR → PICKER → INSPECTOR → TYPE ERROR → SAME IDENTITY

The picker is not the authority layer. Binding law lives in one constitutional registry.

```text
JSON SCHEMA
    ↓
ENTITY REGISTRY
    ↓
BINDING VALIDATOR
    ↓
REACT STATE
    ↓
ICON PICKER
    ↓
PREVIEW GRAPH
    ↓
TRACE
```

## LOCK 01 // IDENTITY

```text
GLYPH CHANGE
→ SAME ENTITY ID
→ SAME TYPE
→ SAME THREADS
→ SAME SCOPE
→ SAME HISTORY
```

## LOCK 02 // REPRESENTATION

```text
glyph
accent
label presentation
layout
animation
= REPRESENTATION ONLY
```

None of those may create a descendant Thing or alter authority.

```text
REPRESENTATION MUTATES
IDENTITY PERSISTS
```

## LOCK 03 // GRAMMAR IS CENTRAL

Do not store binding law independently inside every entity instance.

```text
BINDING_RULES
GLYPH_RULES
EDGE_RULES
```

Otherwise the law will drift between objects.

## LOCK 04 // TREE ≠ THREAD

```text
CONTAINS
THREAD
ROUTES_TO
```

must remain distinct edge kinds. TREE tells us what exists. THREAD is the next module.

## Honesty

- Persistence: EPHEMERAL session storage. Not a ledger.
- Authorization: none. No Talon, no Guardian passport, no auth.
- Bindings: constitutional type grammar only. Not executable work.
- THREAD: typed, not implemented.

DON'T PULL THE GOLDEN CORD.
