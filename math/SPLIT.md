# Split

Subjects `h`, `n`.

```text
mayKnot(CURSOR)   = true
mayKnot(GATOR)    = false
mayKnot(SENTINEL) = false

disjoint(CURSOR, GATOR)    = true
disjoint(CURSOR, SENTINEL) = true
disjoint(GATOR, GATOR)     = false
```

`splitSubjects(h, n)` requires cursor left, node right.

Same `id` in both chambers is ⊥.

`fuse` is ⊥.

Gator and Sentinel are both nodes. They still require **two** talon crossings. Sharing a process in this preview is an honesty issue (ABSENT network), not a license to share a role.
