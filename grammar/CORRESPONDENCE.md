# Correspondence

If lore says it and code cannot fail a test over it, it is decoration.

| Lore | Machine |
| --- | --- |
| THREAD = relationship | session binds human, place, thing, node |
| GLYPH = boundary | grants / forbidden grants / refuseGlyph |
| SCAR = learning | `{ id, event, learning, blocks, requires }` on the Thing |
| WEIGHT = memory | accumulated constraints; never reset |
| KNOT = decision | human-only; receipt payload `decision: KNOT` |
| CURSOR = human | `role: CURSOR`, left door |
| TALON = capability | Gator / Sentinel, right door |
| ADA = composition | not this branch (HANDOFF 002 weave) |

VG06 first line is the Sentinel map law.
Second line is why scars are not deleted.
Third line is why `resetWeight()` fails.
Fourth line is why `nextPull` exists.
