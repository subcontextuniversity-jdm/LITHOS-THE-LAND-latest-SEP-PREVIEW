# LITHOS-THE-LAND-latest-SEP-PREVIEW

The deepest thesis is becoming: Intelligence should move through relationships without acquiring sovereignty over them. Everything else can be derived from that. A model can be powerful without owning the place. A node can execute without becoming the authority. A ledger can prove an event without becoming the entire application.

This branch is **SEP-LATEST** stack spine work: **LOGIN-NEWEST**, **SPLIT-GATE**, **JDM AIRLOCK**, **GATOR** and **SENTINEL** noded work. It is a map and an in-memory proof. It is not the empire.

```text
LORE explains.
LAW constrains.
MATH formalises.
CODE enforces.
NODES execute.
HASHES identify.
RECEIPTS prove.
HUMANS choose.
```

```text
HUMAN → AIRLOCK → SPLIT → PLACE → SCOPE → WORK → RECEIPT → HOME
```

HANDOFF 001 (belonging) and HANDOFF 002 (continuity) live on `cursor/things-we-carry-slice-2b2b`. This folder set is HANDOFF 003's ground: capability without sovereignty.

## Honesty

| Surface | Status |
| --- | --- |
| Authorization | **DEMO-ONLY** |
| Ledger | **IN-MEMORY** |
| Networked Gator/Sentinel | **ABSENT** |
| Passport | **ABSENT** (another agent is on that spec) |
| Native apps | **ABSENT** |
| Conversion `.blend → .glb → .stl` | **ABSENT-HERE** (HANDOFF 002) |

## Run

```bash
npm test
python3 -m http.server 4173 --bind 127.0.0.1
```

- `/` — stack spine map
- `/?airlock=1` — LOGIN-NEWEST split-gate airlock
- `/?proof=1` — noded work: Gator executes, Sentinel maps, weight changes the next pull

## Sorted folders

See `spine/SORT.md`.

| Folder | What it holds |
| --- | --- |
| `spine/` | 2026 stack spine, including the PDF |
| `airlock/` | JDM Airlock, LOGIN-NEWEST, split gate |
| `nodes/gator/` | Executor node. TALON. Cannot knot. |
| `nodes/sentinel/` | Watcher node. Maps. Does not carry. Does not execute. |
| `nodes/noded-work/` | Shared place, disjoint authority |
| `history/` | Mapped past. Not carried. |
| `grammar/` | Locked VISUAL GRAMMAR 06 and correspondence |
| `math/` | Identity, scope, weight, split, receipt |
| `canon/` | Thesis, VG06, HANDOFF 003 |
| `src/` | Code that enforces the above |
| `test/` | Proof the mythology is not decoration |

DON'T PULL THE GOLDEN CORD.
