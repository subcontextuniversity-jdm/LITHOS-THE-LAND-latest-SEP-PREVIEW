# Gator cannot

```text
tie_knot
acquire_sovereignty
leave_place
pull_golden_cord
reset_weight
erase_scar
become_cursor
carry_past_for_human
attest_scope
```

Code:

- `gatorTieKnot()`
- `gatorLeavePlace()`
- `gatorAcquireSovereignty()`
- `execute(..., { kind: "tie_knot" })` fails even if the grant list is sloppy

A Gator that attests its own work is already a fused gate.
