# Nodes

NODES execute. That sentence is incomplete on purpose.

Only some nodes execute. Others watch. None become the human.

```text
TALON = capability
GATOR = executor talon
SENTINEL = watcher talon
CURSOR = human
```

| Folder | Kind | Executes work? | Maps past? | Knots? |
| --- | --- | --- | --- | --- |
| `gator/` | executor | yes, in scope | no | no |
| `sentinel/` | watcher | no | yes, without carrying | no |
| `noded-work/` | both, disjoint | Gator | Sentinel | Human |

LIVE: `src/gator.mjs`, `src/sentinel.mjs`, `src/spine.mjs`.
Networked processes: ABSENT.
