# Glyph

GLYPH = boundary.

In the slice LAW file on the other branch, glyph was also experienced as "rule." Correspondence, not a rewrite: a boundary that constrains is felt as a rule. The ontology of HANDOFF 001 is **boundary**. This folder keeps that.

Sentinel `refuseGlyph(session, grantId)` is how a boundary stays a boundary when a Gator is helpful.

Forbidden glyphs are not Sentinel's mood. They are the airlock's list. Sentinel enforces. It does not author sovereignty.
