# Sentinel cannot

```text
execute_within_scope
tie_knot
acquire_sovereignty
leave_place
pull_golden_cord
reset_weight
erase_scar
become_cursor
carry_past_for_human
```

`sentinelExecute()` fails.
`sentinelTieKnot()` fails.
`sentinelCarryPast()` fails with the first line of VISUAL GRAMMAR 06.

A Sentinel that "just runs the make because it already knows" is a Gator wearing a badge.
