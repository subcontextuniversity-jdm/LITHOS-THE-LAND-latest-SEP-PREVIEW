# Noded work

Work done by nodes, under a human, in a place, without a new emperor.

The proof is `runLoginNewest()` in `src/spine.mjs`.

See `EXECUTE-WITHOUT-SOVEREIGNTY.md`, `RECEIPT-HOME.md`, `BOUNDS.md`.
