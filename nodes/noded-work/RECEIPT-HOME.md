# Receipt, then home

After the knot:

```text
RECEIPT://<first 12 of sha256>
airlock   JDM-AIRLOCK
login     LOGIN-NEWEST
gate      SPLIT
human     human-josh
place     bench
thing     THING://0041
gator     gator-local
sentinel  sentinel-local
decision  KNOT
```

Hash: SHA-256 of RFC 8785 payload. DEMO-ONLY. IN-MEMORY.

Home is not "logged out." Home is the Thing still itself, scars visible, weight intact, carriedForHuman false.

HUMAN → ... → RECEIPT → HOME
