# Execute without sovereignty

HANDOFF 003, derived from the stem thesis.

Something else can work on a Thing without owning the Thing, the Place, or the Human.

## Sequence (LIVE, in-memory)

1. Human Josh arrives at JDM Airlock.
2. Split crossing for Gator. Split crossing for Sentinel. Same human id. Same place. Same Thing.
3. Gator `attempt_make`. Overhang collapses. Scar `scar-overhang` stays. TRACE records the act.
4. Sentinel maps (`carriedForHuman: false`). Applies weight (`requires: require_support`). Attests.
5. Next Gator pull still has `attempt_make` but **requires** support. Retry without the grant fails.
6. Human adds `require_support`. Gator executes. Human knots. Receipt hashed.

Gator never knots.
Sentinel never executes.
Weight never resets.
The past is mapped, not carried.

## What would have been sovereignty

- Gator retries until it "works" and hides the scar.
- Sentinel runs the make because it "already knows."
- A fused login where the model is the user.
- A ledger that becomes the app you have to live inside.
